# src/main/resources/application.properties

spring.application.name=prototip

# === POSTGRESQL (Linux) ===
spring.datasource.url=jdbc:postgresql://localhost:5432/narucivanje
spring.datasource.username=db_narucivanje
spring.datasource.password=yF]5w84(|0z=
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Fix for PostgreSQL CLOB/BLOB errors
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Optional: Connection pool tuning
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.connection-timeout=20000

# UUID v7
spring.jpa.properties.hibernate.id.uuid_generator=org.hibernate.id.uuid.UuidGenerator
spring.jpa.properties.hibernate.id.uuid_generator.style=time

# PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true